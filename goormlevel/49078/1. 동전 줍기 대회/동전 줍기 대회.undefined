n = int(input())
c = list(map(int, input().split())) #동전 가치

dp = [0]*(n+1) #상금
dp[0] = c[0]

max_sum = dp[0]

for i in range(1, n):
	dp[i] = max(dp[i-1]+c[i], c[i])

	max_sum = max(max_sum, dp[i])


print(max_sum)
